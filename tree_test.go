package inu

import (
	"testing"
)

func TestTree_Add(t *testing.T) {
	/*tree := NewTree()
	tree.Add("/", &NodeValue{value: "hello1"})
	tree.Add("/a/b/ccc/dqwe/a/", &NodeValue{value: "hello2"})
	tree.Add("/a/b/dd/dqwe/a/", &NodeValue{value: "hello3"})
	tree.Add("/a/{name}/cc", &NodeValue{value: "hello4"})
	tree.Add("/a/{name:a[0-9]}/dd", &NodeValue{value: "hello5"})
	tree.Add("/a/{name:a[0-9]}/{group:b[A-Z]}", &NodeValue{value: "hello6"})

	paths := []string{"/", "/a/b/dd/dqwe/a/", "/a/b/ccc/dqwe/a/", "/a/a2/dd", "/a/a2/bA", "/a/asdfsadf/cc"}
	for _, path := range paths {
		if nd, param := tree.Find(path, false); nd != nil {
			fmt.Println(path, "\t", nd.value.value, param)
		} else {
			fmt.Println("NOT FOUND")
		}
	}*/
}
